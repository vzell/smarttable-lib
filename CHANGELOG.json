[
    {
        "version": "1.2.0",
        "date": "2026-06-23",
        "summary": "ShowAllEntityData-inspired UI overhaul: three-button sort, per-priority shading, Cc/Rx/Ex filter modifiers, new collapse glyph format, 📊 badge, toolbar action buttons, and performance optimisations.",
        "added": [
            "Three sort-icon buttons (⇅ ▲ ▼) in every sortable column header — always visible; active button highlighted with green text + yellow background + Unicode superscript priority (¹²³…); ⇅ removes the column from the sort stack",
            "Per-priority per-column TD shading — 8 hue families (amber, sky, mint, mauve, peach, teal, lavender, vanilla) with two shades each (0.22 α / 0.44 α); shade alternates on value change within each sorted column; applied simultaneously to all active sort columns",
            "Header TH tinting — sorted column headers receive a solid 60 % tint matching their sort priority hue",
            "Three-zone flex layout for <th> (st-th-inner) — left: label + sort icons; centre: collapse toggle; right: 📊 badge",
            "Cc / Rx / Ex checkbox modifiers on global filter bar — Cc (case-sensitive), Rx (regex mode, default off = literal), Ex (exclude matches)",
            "Cc / Rx / Ex checkbox modifiers per column filter input in the filter row",
            "✕ clear button inset at right edge of global and column filter inputs",
            "Escape key clears the focused filter input (global or column)",
            "st-col-highlight CSS class — light blue (#add8e6) background for column-filter match highlights, distinct from global-filter yellow (st-highlight)",
            "New collapse glyph format: ▶/N/▤ (collapsed) and ◀/N/▤ (expanded); N = sub-row count for cell toggles, multi-row cell count for column header toggles",
            "{count}📊 unique-values badge right-aligned in every filterable column header — replaces ⧨/⧩ button; click opens the filter dropdown",
            "Expand/Collapse ALL button (st-btn-expand-all) in global bar — expands all collapsible columns if any are collapsed, else collapses all",
            "Toggle ALL highlighting button (st-btn-toggle-hl) — shows/hides <mark> elements without rerunning the filter",
            "Clear ALL column filters button (st-btn-clear-col-filters) — resets all ColumnFilter entries to empty",
            "Clear ALL filters button (st-btn-clear-all-filters) — also resets global filter and its checkboxes",
            "DocumentFragment batching in _buildTbody() — all <tr> elements built into a fragment and appended in one operation",
            "Event delegation on <tbody> for cell-toggle clicks — single listener keyed by data-colkey + data-origidx instead of per-button addEventListener",
            "FilterEngine empty-column fast-path — columns with no regex, no meta, no value entries skipped without building a RegExp",
            "SortEngine compiled comparator — sort stack compiled into a single closure before Array.sort(); no per-pair stack traversal",
            "ResizeEngine bodyRows hoisting — snapshot hoisted once before the column loop; hidden rows skipped during measurement"
        ],
        "notes": [
            "st-shade-a and st-shade-b (TR-level primary-column shading) are removed; shading is now applied as per-column TD classes (st-mscol-{P}a/b)",
            "st-th-filter-btn (⧨/⧩) is removed; the 📊 badge (st-uniq-badge) opens the dropdown for all filterable columns",
            "SortEngine.pushSort(colKey, direction?) now accepts an explicit direction param; the ⇅ button calls removeSort(colKey) directly",
            "FilterState gains globalIsRegex: boolean (default false = literal matching); global filter was previously always in regex mode",
            "HEADER_CHROME_PX increased from 56 to ~90 to budget for the three-zone header"
        ]
    },
    {
        "version": "1.1.0",
        "date": "2026-06-23",
        "summary": "Filter row, regex/plain-text toggle, match highlighting, render callback, shading fix, dev workflow.",
        "added": [
            "ColumnDef.render callback — (value, row) => Node | string; custom display per sub-row without affecting sort or filter",
            "Permanent filter row — second <tr> in <thead>, always visible; one text input per filterable column; focus and cursor restored after re-render",
            "ColumnFilter.isRegex — boolean field (default false); when false, filter text is escaped and matched literally; when true, compiled as a full JS regex pattern",
            "Per-column regex toggle button ('.*') in filter row — turns blue when regex mode is active",
            "Match highlighting — active filter matches wrapped in <mark class='st-highlight'> (yellow); both column-level and global patterns contribute; overlapping ranges merged; cells with col.render excluded",
            "escapeRegex() and buildHighlightPattern() exported from filter-engine.js for reuse by the renderer",
            "Four new CSS classes: st-filter-row, st-filter-th, st-filter-input, st-filter-regex-btn",
            "All four adapters implemented: jungleland.js, brucespringsteen-it.js, archive-org.js, brucebase.js",
            "Production userscripts for all four sites (raw.githubusercontent.com @require)",
            "Dev userscripts for all four sites (file:///V:/… @require for Windows WSL2 Chrome/Tampermonkey)",
            "npm run watch — esbuild watch mode rebuilds dist/smarttable.js on every source change"
        ],
        "notes": [
            "First-sort shading was broken: _prevValues was seeded from _currValues, which was empty before the first sort. Fixed by seeding _currValues from pre-sort positions for any untracked column before the snapshot copy.",
            "Dev userscripts use file:///V:/home/vzell/… paths — the V: drive maps the WSL2 filesystem in Windows Chrome/Tampermonkey.",
            "dist/smarttable.js (dev build with inline sourcemaps) is gitignored; run npm run build:dev or npm run watch to generate it locally."
        ]
    },
    {
        "version": "1.0.0",
        "date": "2026-06-23",
        "summary": "Initial release — full-featured interactive table library for Tampermonkey userscripts.",
        "added": [
            "CollapseEngine — per-column and per-cell collapse with configurable peekRows; header badge shows collapsed/total counts",
            "FilterEngine — four-stage pipeline: global regex → column regex → meta predicates (isEmpty, hasImage, hasLink, brokenSrc) → value list; CellMeta cached at build time",
            "SortEngine — multi-column sort stack with Shift+click priority; position-change shading snapshot survives re-renders",
            "ResizeEngine — off-screen ruler auto-resize (measures all visible sub-rows) and drag-to-resize via <colgroup>/<col>; table-layout switches to fixed after first resize",
            "Dropdown — three-section filter dropdown: meta flags, quick regex input, scrollable value list with occurrence counts",
            "CellInspector — walks raw TD/TH element and returns CellMeta: links, images, text strings, isEmpty, hasBrokenSrc flags",
            "TableRenderer — wires all five engines into a fully interactive <table> DOM; sticky header, global regex bar with Exclude/Case toggles, Auto-size columns button, per-column sort/collapse/filter controls",
            "Derived column support — ColumnDef.derivedFrom + ColumnDef.derive(srcStr) pre-computed in _expandRow() at construction so sort, filter, and collapse all see computed values",
            "SmartTable.render({ columns, rows, container, options }) — single public entry point; returns TableRenderer for lifecycle control",
            "src/styles.js — complete default stylesheet for all 37 st- namespaced CSS classes; auto-injects <style id=\"st-styles\"> when loaded via @require, exports STYLES string for GM_addStyle() / bundler use",
            "adapters/jungleland.js — JunglelandAdapter for jungleland.it/html/list.htm; extracts bootleg links by href pattern, parses date from title text, derives year/month/day columns",
            "adapters/brucespringsteen-it.js — BrucespringsteenitAdapter for brucespringsteen.it/DB/records.aspx; dual-mode (official vs unofficial) detected from tipe query param; columnDefs getter switches schema at render time",
            "adapters/archive-org.js — ArchiveOrgAdapter for archive.org bootleg search; page is a React SPA so data is fetched from advancedsearch.php JSON API via GM_xmlhttpRequest; extract() is async, returns a Promise",
            "adapters/brucebase.js — BrucebaseAdapter for brucebase.wikidot.com; splits #page-content on <HR> elements into event blocks; extracts date, venue, eventType, setlist, hasBootleg, hasSetlist from wiki markup",
            "esbuild bundler — npm run build → dist/smarttable.min.js (IIFE, ~32 kB minified, ES2020 target); npm run build:dev → unminified with inline source maps; npm run watch for development",
            ".github/workflows/release.yml — on semver tag push: rebuilds dist, verifies committed dist matches source, verifies package.json version matches tag, creates GitHub Release with dist as asset"
        ],
        "notes": [
            "All adapters set window.XxxAdapter globals instead of using ES module export default — required for Tampermonkey @require loading",
            "archive.org adapter extract() returns a Promise; userscripts must call SmartTable.render() from .then()",
            "dist/smarttable.min.js bundles styles.js as a side-effect import so no separate stylesheet @require is needed"
        ]
    }
]
